"""Shared pytest fixtures.

Implemented incrementally:
    * M3 — pipeline unit-test fixtures (tiny master dataframe, etc.)
    * M4 — agent fixtures (stub-provider helpers)
    * M5 — API fixtures (FastAPI test client)
"""

from __future__ import annotations
